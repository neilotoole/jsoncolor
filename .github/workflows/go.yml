name: Go

on:
  push:
    paths-ignore:
      - '**.md'
  pull_request:
    paths-ignore:
      - '**.md'

jobs:
  build:
    strategy:
      matrix:
        os: [macos-latest, ubuntu-latest, windows-latest]
        # go1.25 is the minimum version specified in go.mod. The newer
        # version verifies the package still compiles and passes tests
        # on recent toolchains.
        go-version: ['1.25', '1.26']

    runs-on: ${{ matrix.os }}

    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-go@v5
      with:
        go-version: ${{ matrix.go-version }}

    - name: Get dependencies
      run: |
        go get -v -t ./...

    - name: Build
      run: go build -v .

    - name: Test
      run: go test -v .
